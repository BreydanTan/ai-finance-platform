# 概念词典 - 技术术语速查手册 📚

**目标读者**: 编程新手、非技术人员
**使用方式**: 遇到不懂的术语时，在此查找简单解释

---

## 📖 如何使用这个词典

每个术语条目包含：
- 💡 **简单解释** - 用日常生活类比
- 🎯 **在项目中的作用** - 具体功能
- 📍 **代码位置** - 在哪里能找到
- 🔗 **相关概念** - 关联的其他术语
- 📚 **学习资源** - 推荐教程（可选）

---

## A - C

### API (Application Programming Interface)

**简单解释**
就像餐厅的菜单。你不需要知道厨房怎么做菜，只需要看菜单点菜。API 就是程序之间交流的"菜单"。

**在本项目中的作用**
项目主要使用 Server Actions（更简单的 API），只有 2 个传统 API：
- `/api/inngest` - 接收后台任务的通知
- `/api/seed` - 生成测试数据

**代码位置**
- API 定义：`app/api/*/route.js`
- Server Actions：`actions/*.js`

**相关概念**
→ [Server Actions](#server-actions)
→ [HTTP 方法](#http-方法)
→ [端点 (Endpoint)](#endpoint-端点)

---

### ArcJet

**简单解释**
网站的保安系统。它能识别并阻止恶意访问，比如机器人攻击或者有人疯狂点击按钮。

**在项目中的作用**
- 🤖 **Bot 检测** - 识别并阻止机器人
- 🛡️ **Shield** - 防止常见攻击
- ⏱️ **速率限制** - 每小时最多 10 次创建账户请求

**代码位置**
- 配置：`lib/arcjet.js`
- 使用示例：`actions/dashboard.js:75`（创建账户时检查）

**相关概念**
→ [中间件 (Middleware)](#middleware-中间件)
→ [速率限制 (Rate Limiting)](#rate-limiting-速率限制)

**学习资源**
- [ArcJet 官方文档](https://docs.arcjet.com)

---

### async/await

**简单解释**
就像你点外卖：
- `async` 表示"这个操作需要等待"
- `await` 表示"在这里等外卖送到"

你不会一直盯着门口，但到这一步时必须等外卖送到才能继续。

**在项目中的作用**
所有数据库操作、API 调用都需要等待：

```javascript
// ❌ 错误：不等待，数据还没来就继续了
const data = getUserAccounts();

// ✅ 正确：等待数据返回
const data = await getUserAccounts();
```

**代码位置**
几乎所有 Server Actions 和服务器组件都使用

**相关概念**
→ [Promise](#promise)
→ [Server Actions](#server-actions)

**学习资源**
- 类比：就像你在银行排队，轮到你之前必须等待

---

### App Router

**简单解释**
Next.js 15 的新路由系统。就像一个智能地图，告诉程序"当用户访问 /dashboard 时，显示哪个页面"。

**在项目中的作用**
- `app/` 文件夹结构直接对应 URL
- `app/(main)/dashboard/page.jsx` → 访问 `/dashboard`
- `app/(auth)/sign-in/page.jsx` → 访问 `/sign-in`

**代码位置**
- 所有路由：`app/` 文件夹
- 参考：`project-mastery/analysis/00-project-overview.md`

**相关概念**
→ [路由 (Route)](#route-路由)
→ [服务器组件 (Server Component)](#server-component-服务器组件)

---

### Authentication (认证)

**简单解释**
验证"你是谁"。就像你进小区需要刷门禁卡，网站需要确认你的身份。

**在项目中的作用**
使用 Clerk 服务处理用户登录：
- 📧 邮箱 + 密码登录
- 🔐 Google OAuth
- 🐙 GitHub OAuth

**代码位置**
- 登录页面：`app/(auth)/sign-in/[[...sign-in]]/page.jsx`
- 用户检查：`lib/checkUser.js`
- 中间件保护：`middleware.js`

**相关概念**
→ [Clerk](#clerk)
→ [Session (会话)](#session-会话)
→ [中间件 (Middleware)](#middleware-中间件)

---

### Authorization (授权)

**简单解释**
验证"你能做什么"。就像你有小区门禁卡（认证），但不能进别人家（授权）。

**在项目中的作用**
- 用户只能看到自己的账户
- 不能修改别人的交易记录
- 通过 `userId` 过滤数据

**代码位置**
```javascript
// actions/dashboard.js:29
const user = await db.user.findUnique({
  where: { clerkUserId: userId },
});

// 只返回这个用户的账户
const accounts = await db.account.findMany({
  where: { userId: user.id },
});
```

**相关概念**
→ [Authentication (认证)](#authentication-认证)

---

### Balance (余额)

**简单解释**
账户里有多少钱。就像你钱包里的现金数量。

**在项目中的作用**
- 每个账户 (Account) 有一个 `balance` 字段
- 收入 → 余额增加
- 支出 → 余额减少
- 类型：`Decimal` (支持小数点)

**代码位置**
- 数据库字段：`prisma/schema.prisma:14`
- 更新余额：`actions/transaction.js:63-78`

**相关概念**
→ [Account (账户)](#account-账户)
→ [Transaction (交易)](#transaction-交易)

---

### Budget (预算)

**简单解释**
你给自己设定的"这个月最多花多少钱"。就像你告诉自己"这个月购物预算 3000 元"。

**在项目中的作用**
- 为每个账户设置月度预算
- 实时追踪已花费金额
- 超过 90% 时发邮件警报

**代码位置**
- 数据模型：`prisma/schema.prisma:60-71`
- 设置预算：`actions/budget.js:68-112`
- 警报检查：`lib/inngest/function.js:171-230`

**相关概念**
→ [Account (账户)](#account-账户)
→ [警报 (Alert)](#alert-警报)
→ [Inngest](#inngest)

---

### CDN (Content Delivery Network)

**简单解释**
内容分发网络。就像全国各地的仓库，把网站内容存在离用户最近的地方，访问更快。

**在项目中的作用**
Vercel 自动提供 CDN：
- 静态文件（图片、CSS）分发到全球节点
- 用户访问时从最近的节点加载

**代码位置**
自动处理，无需配置

**相关概念**
→ [Vercel](#vercel)
→ [Edge Network](#edge-network)

---

### Clerk

**简单解释**
专业的"门卫服务"。你不需要自己写登录系统，Clerk 帮你搞定用户注册、登录、密码管理等所有事情。

**在项目中的作用**
- 🔐 处理用户注册和登录
- 🔄 提供 OAuth（Google、GitHub 登录）
- 👤 管理用户信息
- 🎫 生成和验证登录凭证

**代码位置**
- 配置：`.env` 中的 `CLERK_*` 变量
- 登录页：`app/(auth)/sign-in/[[...sign-in]]/page.jsx`
- 获取当前用户：`const { userId } = await auth()`

**相关概念**
→ [Authentication (认证)](#authentication-认证)
→ [Session (会话)](#session-会话)

**学习资源**
- [Clerk 官方文档](https://clerk.com/docs)

---

### Client Component (客户端组件)

**简单解释**
需要在用户浏览器中运行的代码。就像手机 App，下载到你手机上运行。

**在项目中的作用**
需要交互的组件使用客户端组件：
- 表单输入
- 按钮点击
- 动画效果

**识别方法**
文件顶部有 `"use client"` 标记

**代码位置**
```javascript
// app/(main)/transaction/_components/transaction-form.jsx:1
"use client";

export default function TransactionForm() {
  // 可以使用 useState, onClick 等
}
```

**相关概念**
→ [Server Component (服务器组件)](#server-component-服务器组件)
→ [React](#react)

---

### Component (组件)

**简单解释**
网页的"积木块"。就像乐高积木，每个组件是一个小块，组合起来变成完整网页。

**在项目中的作用**
- 按钮组件：`components/ui/button.jsx`
- 表单组件：`transaction-form.jsx`
- 页面组件：`app/(main)/dashboard/page.jsx`

**代码位置**
- UI 基础组件：`components/ui/`
- 业务组件：`components/`
- 页面组件：`app/` 文件夹中的 `page.jsx`

**相关概念**
→ [React](#react)
→ [Props](#props)

---

### CRUD

**简单解释**
数据操作的四大金刚：
- **C**reate - 创建（添加新数据）
- **R**ead - 读取（查看数据）
- **U**pdate - 更新（修改数据）
- **D**elete - 删除（删除数据）

**在项目中的作用**
每个功能都是 CRUD 操作：

| 操作 | 示例 | 代码位置 |
|-----|------|---------|
| Create | 创建新交易 | `actions/transaction.js:18-100` |
| Read | 查看交易列表 | `actions/account.js:11-64` |
| Update | 修改交易 | `actions/transaction.js:105-176` |
| Delete | 删除交易 | `actions/transaction.js:181-226` |

**相关概念**
→ [Prisma](#prisma)
→ [数据库 (Database)](#database-数据库)

---

## D - F

### Database (数据库)

**简单解释**
存储数据的仓库。就像一个超级整齐的文件柜，每个抽屉都有标签，可以快速找到你要的文件。

**在项目中的作用**
使用 PostgreSQL 数据库存储：
- 👤 用户信息
- 💰 账户数据
- 📝 交易记录
- 📊 预算设置

**代码位置**
- 数据库结构定义：`prisma/schema.prisma`
- 连接配置：`.env` 中的 `DATABASE_URL`
- 详细分析：`project-mastery/analysis/01-database-analysis.md`

**相关概念**
→ [PostgreSQL](#postgresql)
→ [Prisma](#prisma)
→ [Table (表)](#table-表)

---

### date-fns

**简单解释**
处理日期的工具箱。就像一个万年历 + 计算器，帮你计算"3 天后是几号"、"两个日期相差多少天"等。

**在项目中的作用**
- 格式化日期显示："2024-11-16" → "2024年11月16日"
- 计算日期范围："这个月的第一天"、"下个月同一天"
- 日期比较和计算

**代码位置**
```javascript
import { format, addDays } from "date-fns";

// 格式化
format(new Date(), "yyyy-MM-dd"); // "2024-11-16"

// 计算
addDays(new Date(), 7); // 7天后
```

**相关概念**
→ [Date (日期)](#date-日期)

**学习资源**
- [date-fns 文档](https://date-fns.org)

---

### Decimal

**简单解释**
精确的小数。普通数字算钱会有误差（0.1 + 0.2 = 0.30000000004），Decimal 保证精确。

**在项目中的作用**
所有金额字段使用 Decimal 类型：
- 账户余额
- 交易金额
- 预算金额

**代码位置**
```javascript
// prisma/schema.prisma
model Account {
  balance Decimal @default(0) @db.Decimal(10, 2)
  //                           ↑ 10位数字，2位小数
}
```

**相关概念**
→ [Prisma](#prisma)
→ [Balance (余额)](#balance-余额)

---

### Endpoint (端点)

**简单解释**
API 的具体地址。就像每个饭店有不同的窗口：1号窗口点餐，2号窗口取餐。

**在项目中的作用**
主要使用 Server Actions（不是传统端点），但有这些 API 端点：
- `/api/inngest` - 后台任务通知
- `/api/seed` - 生成测试数据

**代码位置**
- API 路由：`app/api/*/route.js`
- Server Actions：`actions/` 文件夹

**相关概念**
→ [API](#api-application-programming-interface)
→ [Server Actions](#server-actions)

---

### Environment Variables (环境变量)

**简单解释**
项目的"配置文件"，存放密码、密钥等敏感信息。就像你的保险柜，不能公开。

**在项目中的作用**
存储所有敏感配置：
- 数据库连接地址
- API 密钥
- 认证密钥

**代码位置**
- 配置文件：`.env`（不提交到 Git）
- 使用示例：`process.env.DATABASE_URL`
- 完整列表：`project-mastery/specifications/PROJECT_SPEC_CN.md` 第7章

**相关概念**
→ [.env 文件](#env-文件)

**重要提示**
⚠️ **永远不要**把 `.env` 文件提交到 Git！

---

### .env 文件

**简单解释**
存放密钥的文件。文件名就叫 `.env`，放在项目根目录。

**在项目中的作用**
```bash
# .env 文件内容示例
DATABASE_URL="postgresql://..."
CLERK_SECRET_KEY="sk_test_..."
GEMINI_API_KEY="..."
```

**代码位置**
- 文件位置：项目根目录 `/ai-finance-platform/.env`
- 模板文件：`.env.example`

**相关概念**
→ [Environment Variables (环境变量)](#environment-variables-环境变量)

**安全提示**
- ✅ 加入 `.gitignore`
- ✅ 每个开发者自己创建
- ❌ 不要发送给别人
- ❌ 不要提交到 GitHub

---

### Error Handling (错误处理)

**简单解释**
当出错时怎么办。就像你点外卖，如果店家打烊了，App 会提示你"这家店暂时休息"，而不是直接卡死。

**在项目中的作用**
多层错误处理：
1. **前端验证** - 防止用户输入错误
2. **Server Action** - try/catch 捕获错误
3. **Error Boundary** - 页面级错误捕获
4. **Toast 提示** - 用户友好的错误提示

**代码位置**
```javascript
// actions/transaction.js:25-32
try {
  const transaction = await createTransaction(data);
  return { success: true, data: transaction };
} catch (error) {
  console.error("Transaction error:", error);
  throw new Error("创建交易失败");
}
```

**相关概念**
→ [Try/Catch](#trycatch)
→ [Toast](#toast)

---

### Foreign Key (外键)

**简单解释**
数据表之间的"连接线"。就像你的学生证上写着"XX学校"，这就是一个指向学校表的外键。

**在项目中的作用**
建立表之间的关系：
- Transaction 表有 `userId` 外键 → 指向 User 表
- Transaction 表有 `accountId` 外键 → 指向 Account 表

**代码位置**
```javascript
// prisma/schema.prisma
model Transaction {
  userId    String
  user      User    @relation(fields: [userId], references: [id])
  //        ↑ 关系    ↑ 本表字段      ↑ 关联到 User.id
}
```

**相关概念**
→ [关系 (Relation)](#relation-关系)
→ [Database (数据库)](#database-数据库)

---

### Form (表单)

**简单解释**
网页上让你填写信息的区域。就像纸质表格，填写姓名、电话等信息。

**在项目中的作用**
项目中的表单：
- 创建账户表单
- 添加交易表单
- 设置预算表单

**代码位置**
- 交易表单：`app/(main)/transaction/_components/transaction-form.jsx`
- 使用 React Hook Form 管理状态
- 使用 Zod 验证输入

**相关概念**
→ [React Hook Form](#react-hook-form)
→ [Zod](#zod)
→ [Validation (验证)](#validation-验证)

---

### Function (函数)

**简单解释**
执行特定任务的代码块。就像一个小机器人，你给它输入，它完成任务后给你输出。

**在项目中的作用**
所有功能都是函数：
```javascript
// 创建账户的函数
async function createAccount(data) {
  // 输入：账户数据
  // 任务：保存到数据库
  // 输出：新创建的账户
  return newAccount;
}
```

**代码位置**
- Server Actions：`actions/*.js`
- 组件函数：所有 `.jsx` 文件
- 工具函数：`lib/*.js`

**相关概念**
→ [Server Actions](#server-actions)
→ [async/await](#asyncawait)

---

## G - I

### Gemini AI

**简单解释**
Google 的人工智能服务。就像一个超级聪明的助手，能看懂图片、理解文字。

**在项目中的作用**
**收据扫描功能**：
1. 用户上传收据图片
2. 发送给 Gemini AI
3. AI 识别并提取：金额、日期、商家名称、分类
4. 自动填充到表单

**代码位置**
- 扫描函数：`actions/transaction.js:231-291`
- API 密钥配置：`.env` 中的 `GEMINI_API_KEY`

**使用示例**
```javascript
const result = await scanReceipt(imageFile);
// {
//   amount: 42.50,
//   date: "2024-11-16",
//   description: "星巴克",
//   category: "food"
// }
```

**相关概念**
→ [OCR](#ocr)
→ [AI](#ai-人工智能)

**学习资源**
- [Google AI Studio](https://makersuite.google.com)

---

### Git

**简单解释**
代码的"时光机"。可以：
- 📸 保存代码的每个版本（像拍照）
- ⏮️ 回到任何历史版本
- 🌿 创建平行宇宙（分支）测试新功能
- 🤝 多人协作不会冲突

**在项目中的作用**
- 保存代码历史
- 团队协作开发
- 部署到 Vercel

**常用命令**
```bash
git status           # 查看状态
git add .            # 添加所有更改
git commit -m "描述"  # 保存版本
git push             # 上传到 GitHub
git pull             # 下载最新代码
```

**相关概念**
→ [GitHub](#github)
→ [版本控制](#version-control-版本控制)

**学习资源**
- 类比：Git 就是你写论文时的"另存为"功能，但更强大

---

### GitHub

**简单解释**
代码的"网盘"。就像百度网盘存文件，GitHub 存代码，而且可以多人协作。

**在项目中的作用**
- 存储项目代码
- 版本历史记录
- 团队协作
- 连接到 Vercel 自动部署

**相关概念**
→ [Git](#git)
→ [Repository (仓库)](#repository-仓库)

---

### Hook (React Hooks)

**简单解释**
React 的"魔法工具"。就像给组件安装的各种功能插件。

**在项目中的作用**

常用 Hooks：
- `useState` - 记住数据（像变量）
- `useEffect` - 在特定时机执行代码
- `useForm` - 管理表单（自定义 Hook）
- `useFetch` - 处理数据请求（自定义 Hook）

**代码位置**
```javascript
// 使用 useState
const [count, setCount] = useState(0);

// 使用自定义 Hook
const { fn, loading } = useFetch(createAccount);
```

**相关概念**
→ [useState](#usestate)
→ [useEffect](#useeffect)
→ [useFetch](#usefetch)

**学习资源**
- [React Hooks 官方文档](https://react.dev/reference/react)

---

### HTTP Methods (HTTP 方法)

**简单解释**
向服务器发请求时的"动作类型"：
- **GET** - 获取（"给我看菜单"）
- **POST** - 创建（"我要点这个菜"）
- **PUT** - 更新（"把我的菜换成另一个"）
- **DELETE** - 删除（"取消这道菜"）

**在项目中的作用**
虽然项目主要用 Server Actions，但底层还是 HTTP：
- 查询数据 → GET
- 创建交易 → POST
- 修改交易 → PUT
- 删除交易 → DELETE

**相关概念**
→ [API](#api-application-programming-interface)
→ [CRUD](#crud)

---

### Inngest

**简单解释**
定时任务助手。就像你设置的闹钟和提醒事项，可以定时执行任务。

**在项目中的作用**

自动执行的任务：
- ⏰ **每天午夜** - 处理循环交易
- ⏰ **每6小时** - 检查预算警报
- ⏰ **每月1号** - 生成月度报告

**代码位置**
- 任务定义：`lib/inngest/function.js`
- API 端点：`app/api/inngest/route.js`
- 详细分析：`project-mastery/analysis/02-backend-analysis.md`

**工作流程**
```
1. Inngest 定时触发
   ↓
2. 调用 /api/inngest
   ↓
3. 执行对应的任务函数
   ↓
4. 任务完成，等待下次触发
```

**相关概念**
→ [Cron Job](#cron-job)
→ [Background Jobs (后台作业)](#background-jobs-后台作业)

**学习资源**
- [Inngest 文档](https://www.inngest.com/docs)

---

### JWT (JSON Web Token)

**简单解释**
数字身份证。一串加密的文本，证明"你是谁"和"你能做什么"。

**在项目中的作用**
Clerk 生成 JWT 存储在浏览器中：
- 用户登录后获得 JWT
- 每次请求都携带 JWT
- 服务器验证 JWT 确认身份

**代码位置**
Clerk 自动处理，无需手动操作

**相关概念**
→ [Clerk](#clerk)
→ [Authentication (认证)](#authentication-认证)
→ [Token](#token)

---

## L - N

### Loading State (加载状态)

**简单解释**
数据还没来时显示的"请稍候"。就像你点外卖后显示"骑手正在赶来"。

**在项目中的作用**
显示加载提示，改善用户体验：

```javascript
const { fn, loading } = useFetch(createAccount);

return (
  <button disabled={loading}>
    {loading ? "创建中..." : "创建账户"}
  </button>
);
```

**代码位置**
- useFetch Hook：`hooks/use-fetch.js`
- 加载页面：`app/(main)/dashboard/loading.jsx`

**相关概念**
→ [useFetch](#usefetch)
→ [Suspense](#suspense)

---

### Middleware (中间件)

**简单解释**
请求的"检查站"。用户访问页面前，先经过中间件检查，就像机场安检。

**在项目中的作用**

中间件检查：
1. 用户是否登录？
   - 否 → 重定向到登录页
   - 是 → 允许访问
2. 记录访问日志
3. 检查安全威胁

**代码位置**
```javascript
// middleware.js
export default clerkMiddleware((auth, req) => {
  const isProtectedRoute = createRouteMatcher([
    "/dashboard(.*)",
    "/account(.*)",
  ]);

  if (isProtectedRoute(req)) {
    auth().protect(); // 要求登录
  }
});
```

**相关概念**
→ [Clerk](#clerk)
→ [Authentication (认证)](#authentication-认证)

---

### Migration (迁移)

**简单解释**
数据库的"装修图纸"。记录了数据库结构的每次改动，可以在不同环境重建相同的数据库。

**在项目中的作用**
- 创建表
- 添加字段
- 修改字段类型
- 保留历史记录

**代码位置**
- 迁移文件：`prisma/migrations/`
- 生成迁移：`npx prisma migrate dev --name 描述`

**使用示例**
```bash
# 修改了 schema.prisma 后
npx prisma migrate dev --name add_tags_field

# Prisma 会自动：
# 1. 生成迁移文件
# 2. 应用到数据库
# 3. 更新 Prisma 客户端
```

**相关概念**
→ [Prisma](#prisma)
→ [Database (数据库)](#database-数据库)

---

### Model (模型)

**简单解释**
数据的"蓝图"。定义了某种数据长什么样，有哪些字段。

**在项目中的作用**

项目的4个主要模型：
- **User** - 用户（姓名、邮箱）
- **Account** - 账户（名称、类型、余额）
- **Transaction** - 交易（金额、日期、分类）
- **Budget** - 预算（金额、月份）

**代码位置**
```javascript
// prisma/schema.prisma
model User {
  id           String   @id @default(cuid())
  email        String   @unique
  name         String?
  accounts     Account[]
  transactions Transaction[]
}
```

**相关概念**
→ [Prisma](#prisma)
→ [Schema](#schema)
→ [Table (表)](#table-表)

---

### Next.js

**简单解释**
React 的"豪华版"。在 React 基础上添加了很多实用功能：路由、服务器渲染、优化等。

**在项目中的作用**
整个项目基于 Next.js 15 构建：
- 📂 App Router - 文件夹即路由
- 🖥️ Server Components - 服务器组件
- ⚡ Server Actions - 服务器函数
- 🎨 自动优化 - 图片、字体等

**代码位置**
- 配置：`next.config.mjs`
- 所有代码都在 Next.js 框架内运行

**相关概念**
→ [React](#react)
→ [App Router](#app-router)
→ [Server Component](#server-component-服务器组件)

**学习资源**
- [Next.js 官方教程](https://nextjs.org/learn)

---

### npm (Node Package Manager)

**简单解释**
JavaScript 的"应用商店"。可以下载和管理别人写好的代码包。

**在项目中的作用**

常用命令：
```bash
npm install        # 安装所有依赖
npm run dev        # 启动开发服务器
npm run build      # 构建生产版本
npm start          # 运行生产版本
```

**代码位置**
- 依赖列表：`package.json`
- 安装的包：`node_modules/` 文件夹

**相关概念**
→ [package.json](#packagejson)
→ [Dependencies (依赖)](#dependencies-依赖)

---

## O - R

### OAuth

**简单解释**
"用其他账号登录"功能。就像很多网站可以"用微信登录"、"用QQ登录"。

**在项目中的作用**
支持第三方登录：
- 🔵 Google 账号登录
- 🐙 GitHub 账号登录

**代码位置**
Clerk 自动处理 OAuth 流程

**相关概念**
→ [Clerk](#clerk)
→ [Authentication (认证)](#authentication-认证)

---

### OCR (Optical Character Recognition)

**简单解释**
图片文字识别。就像你拍照后，AI 能把图片上的文字"读"出来。

**在项目中的作用**
**收据扫描功能**：
1. 拍照上传收据
2. Gemini AI 识别收据上的文字
3. 提取金额、日期、商家等信息

**代码位置**
- `actions/transaction.js:231-291`

**相关概念**
→ [Gemini AI](#gemini-ai)
→ [Receipt Scanning (收据扫描)](#receipt-scanning-收据扫描)

---

### ORM (Object-Relational Mapping)

**简单解释**
把数据库的"表格"转换成代码中的"对象"。就像翻译官，让你用 JavaScript 和数据库对话。

**在项目中的作用**
使用 Prisma ORM：

```javascript
// ❌ 不用 Prisma，要写 SQL
await db.query("SELECT * FROM users WHERE id = $1", [userId]);

// ✅ 用 Prisma，更简单
await db.user.findUnique({ where: { id: userId } });
```

**相关概念**
→ [Prisma](#prisma)
→ [Database (数据库)](#database-数据库)

---

### package.json

**简单解释**
项目的"说明书"。记录了项目名称、版本、需要哪些依赖包、有哪些命令等。

**在项目中的作用**
```json
{
  "name": "ai-finance-platform",
  "version": "0.1.0",
  "scripts": {
    "dev": "next dev",      // npm run dev 命令
    "build": "next build"   // npm run build 命令
  },
  "dependencies": {
    "next": "15.0.3",       // 需要的包和版本
    "react": "19.0.0-rc"
  }
}
```

**代码位置**
- 文件位置：项目根目录 `package.json`

**相关概念**
→ [npm](#npm-node-package-manager)
→ [Dependencies (依赖)](#dependencies-依赖)

---

### PostgreSQL

**简单解释**
强大的开源数据库。就像一个超级智能的Excel表格，可以存储海量数据，快速查找。

**在项目中的作用**
存储所有项目数据：
- 用户信息
- 账户和余额
- 交易记录
- 预算设置

**代码位置**
- 连接配置：`.env` 中的 `DATABASE_URL`
- 通过 Prisma 访问，不直接写 SQL

**相关概念**
→ [Database (数据库)](#database-数据库)
→ [Prisma](#prisma)
→ [Supabase](#supabase)

**学习资源**
- 类比：PostgreSQL 是图书馆，Prisma 是图书管理员

---

### Prisma

**简单解释**
数据库的"翻译官"。让你用 JavaScript 代码操作数据库，不需要写 SQL。

**在项目中的作用**

所有数据库操作都通过 Prisma：
```javascript
// 查询
const user = await db.user.findUnique({ where: { id } });

// 创建
const account = await db.account.create({ data: {...} });

// 更新
await db.transaction.update({ where: { id }, data: {...} });

// 删除
await db.transaction.delete({ where: { id } });
```

**代码位置**
- Schema 定义：`prisma/schema.prisma`
- 客户端初始化：`lib/prisma.js`
- 使用：所有 `actions/*.js` 文件

**相关概念**
→ [ORM](#orm-object-relational-mapping)
→ [Database (数据库)](#database-数据库)
→ [Model (模型)](#model-模型)

**学习资源**
- [Prisma 官方文档](https://www.prisma.io/docs)

---

### Promise

**简单解释**
JavaScript 的"欠条"。代表一个未来会完成的操作，就像你点外卖得到的订单号，外卖还没到，但会来。

**在项目中的作用**
所有异步操作都返回 Promise：
```javascript
// Promise 有三种状态：
// Pending - 进行中（外卖在路上）
// Fulfilled - 成功（外卖送到了）
// Rejected - 失败（外卖被取消）

getUserAccounts()  // 返回 Promise
  .then(accounts => console.log(accounts))
  .catch(error => console.error(error));

// 或者用 async/await
const accounts = await getUserAccounts();
```

**相关概念**
→ [async/await](#asyncawait)

---

### Props

**简单解释**
组件的"参数"。就像你给函数传参数，给组件传 Props。

**在项目中的作用**
父组件向子组件传递数据：

```javascript
// 父组件
<AccountCard
  name="主账户"     // Props
  balance={5000}   // Props
/>

// 子组件接收
function AccountCard({ name, balance }) {
  return <div>{name}: ¥{balance}</div>;
}
```

**代码位置**
所有组件都使用 Props 传递数据

**相关概念**
→ [Component (组件)](#component-组件)
→ [React](#react)

---

### Radix UI

**简单解释**
高质量的 UI 组件库。提供按钮、对话框、下拉菜单等预制组件，而且无障碍友好。

**在项目中的作用**
项目的所有基础 UI 组件都基于 Radix UI：
- Button（按钮）
- Dialog（对话框）
- Select（下拉选择）
- Drawer（抽屉）

**代码位置**
- UI 组件：`components/ui/`
- 配置：`package.json` 中的 `@radix-ui/*` 依赖

**相关概念**
→ [Component (组件)](#component-组件)
→ [UI](#ui-user-interface)

**学习资源**
- [Radix UI 文档](https://www.radix-ui.com)

---

### Rate Limiting (速率限制)

**简单解释**
防止有人"刷单"。就像银行限制"每天最多转账10次"，防止异常操作。

**在项目中的作用**
ArcJet 提供速率限制：
- 创建账户：10次/小时
- 创建交易：20次/小时

**代码位置**
```javascript
// actions/dashboard.js:75
const { success, pending } = await arcjet.protect(req, {
  requested: 1,
});

if (!success) {
  throw new Error("Too many requests");
}
```

**相关概念**
→ [ArcJet](#arcjet)
→ [安全 (Security)](#security-安全)

---

### React

**简单解释**
构建网页界面的 JavaScript 库。就像乐高积木，用组件搭建页面。

**在项目中的作用**
整个前端基于 React 构建：
- 组件化开发
- 响应式界面
- 虚拟 DOM

**代码位置**
所有 `.jsx` 文件都是 React 组件

**相关概念**
→ [Component (组件)](#component-组件)
→ [Next.js](#nextjs)
→ [JSX](#jsx)

**学习资源**
- [React 官方教程](https://react.dev/learn)

---

### React Hook Form

**简单解释**
表单管理神器。帮你处理表单的所有麻烦事：输入、验证、提交、错误提示。

**在项目中的作用**
所有表单都用它管理：

```javascript
const { register, handleSubmit, formState: { errors } } = useForm({
  resolver: zodResolver(transactionSchema),
});

<input {...register("amount")} />
{errors.amount && <span>{errors.amount.message}</span>}
```

**代码位置**
- 交易表单：`app/(main)/transaction/_components/transaction-form.jsx:34`
- 账户表单：`components/create-account-drawer.jsx`

**相关概念**
→ [Form (表单)](#form-表单)
→ [Zod](#zod)
→ [Validation (验证)](#validation-验证)

**学习资源**
- [React Hook Form 文档](https://react-hook-form.com)

---

### Recharts

**简单解释**
图表库。轻松创建柱状图、折线图、饼图等数据可视化图表。

**在项目中的作用**
展示财务数据：
- 收入/支出趋势图
- 分类占比饼图
- 月度对比柱状图

**代码位置**
```javascript
import { BarChart, Bar, XAxis, YAxis } from "recharts";

<BarChart data={transactions}>
  <Bar dataKey="amount" fill="#8884d8" />
</BarChart>
```

**相关概念**
→ [数据可视化](#data-visualization-数据可视化)

**学习资源**
- [Recharts 文档](https://recharts.org)

---

### Recurring (循环/定期)

**简单解释**
重复发生的事情。就像你的月租、工资，每个月固定时间发生。

**在项目中的作用**
**循环交易功能**：
- 设置每月房租支出
- 设置每月工资收入
- 自动在指定日期创建交易

**代码位置**
- 数据字段：`prisma/schema.prisma` Transaction 模型
  - `isRecurring` - 是否循环
  - `recurringInterval` - 循环间隔（DAILY/WEEKLY/MONTHLY/YEARLY）
  - `nextRecurringDate` - 下次执行日期
- 处理函数：`lib/inngest/function.js:30-110`

**相关概念**
→ [Transaction (交易)](#transaction-交易)
→ [Inngest](#inngest)

---

### Relation (关系)

**简单解释**
数据表之间的连接。就像"用户"拥有多个"账户"，这就是一对多关系。

**在项目中的作用**

项目中的关系：
- User → Account (一对多) - 一个用户有多个账户
- Account → Transaction (一对多) - 一个账户有多笔交易
- User → Transaction (一对多) - 一个用户有多笔交易

**代码位置**
```javascript
// prisma/schema.prisma
model User {
  id           String        @id
  accounts     Account[]     // 一对多关系
  transactions Transaction[]
}
```

**相关概念**
→ [Foreign Key (外键)](#foreign-key-外键)
→ [Prisma](#prisma)

---

### Resend

**简单解释**
专业的邮件发送服务。就像一个可靠的邮递员,帮你发送各种通知邮件。

**在项目中的作用**
发送通知邮件：
- 预算警报（超过90%时）
- 月度财务报告

**代码位置**
- 发送邮件：`actions/send-email.js`
- API 密钥：`.env` 中的 `RESEND_API_KEY`

**使用示例**
```javascript
await resend.emails.send({
  from: "noreply@yourapp.com",
  to: user.email,
  subject: "预算警报",
  html: `您的预算已使用 ${percentage}%`,
});
```

**相关概念**
→ [Email (邮件)](#email-邮件)
→ [Inngest](#inngest)

**学习资源**
- [Resend 文档](https://resend.com/docs)

---

### revalidatePath

**简单解释**
清除缓存。告诉 Next.js "这个页面的数据变了，重新获取"。

**在项目中的作用**
每次修改数据后调用：

```javascript
// actions/transaction.js:95
await db.transaction.create({ data });

// 清除仪表板缓存，强制重新加载
revalidatePath("/dashboard");
revalidatePath(`/account/${accountId}`);
```

**代码位置**
几乎所有 Server Actions 都使用

**相关概念**
→ [Server Actions](#server-actions)
→ [缓存 (Cache)](#cache-缓存)

---

### Route (路由)

**简单解释**
网址和页面的对应关系。就像地图上的路线，访问不同网址显示不同页面。

**在项目中的作用**

| 路由 | 对应文件 | 说明 |
|-----|---------|------|
| `/` | `app/page.jsx` | 首页 |
| `/dashboard` | `app/(main)/dashboard/page.jsx` | 仪表板 |
| `/account/123` | `app/(main)/account/[id]/page.jsx` | 账户详情 |
| `/transaction/create` | `app/(main)/transaction/create/page.jsx` | 创建交易 |

**代码位置**
- 所有路由：`app/` 文件夹结构

**相关概念**
→ [App Router](#app-router)
→ [Next.js](#nextjs)

---

## S - Z

### Schema

**简单解释**
数据结构的"设计图纸"。定义了数据库有哪些表、每个表有哪些字段。

**在项目中的作用**

Prisma Schema 定义数据库结构：
```prisma
model User {
  id    String @id @default(cuid())
  email String @unique
  name  String?
}
```

Zod Schema 定义数据验证规则：
```javascript
const schema = z.object({
  email: z.string().email("无效邮箱"),
  amount: z.number().positive("金额必须为正"),
});
```

**代码位置**
- 数据库 Schema：`prisma/schema.prisma`
- 验证 Schema：`app/lib/schema.js`

**相关概念**
→ [Prisma](#prisma)
→ [Zod](#zod)
→ [Model (模型)](#model-模型)

---

### Security (安全)

**简单解释**
保护网站和用户数据安全。就像给房子装锁、装监控。

**在项目中的作用**

多层安全防护：
1. **认证** - Clerk 验证用户身份
2. **授权** - 检查用户权限
3. **速率限制** - ArcJet 防止滥用
4. **输入验证** - Zod 防止恶意输入
5. **HTTPS** - 加密传输
6. **环境变量** - 保护敏感信息

**代码位置**
- 详细分析：`project-mastery/analysis/04-security-analysis.md`

**相关概念**
→ [ArcJet](#arcjet)
→ [Clerk](#clerk)
→ [Validation (验证)](#validation-验证)

---

### Server Actions

**简单解释**
Next.js 15 的"魔法"。可以在服务器上运行的函数，但能从客户端直接调用，不需要写 API。

**在项目中的作用**

替代传统 API Routes：
```javascript
// actions/dashboard.js
"use server";  // 标记为服务器函数

export async function createAccount(data) {
  // 这里的代码在服务器运行
  const account = await db.account.create({ data });
  return account;
}

// 在客户端组件直接调用
const account = await createAccount(formData);
```

**代码位置**
- 所有 Server Actions：`actions/` 文件夹

**相关概念**
→ [API](#api-application-programming-interface)
→ [Next.js](#nextjs)

**优势**
✅ 类型安全
✅ 代码简洁
✅ 自动优化

---

### Server Component (服务器组件)

**简单解释**
在服务器上渲染的 React 组件。就像餐厅后厨准备好菜再端出来，不是在桌上现做。

**在项目中的作用**

默认所有组件都是服务器组件：
```javascript
// app/(main)/dashboard/page.jsx
// 这是服务器组件（无需标记）

export default async function DashboardPage() {
  // 可以直接访问数据库
  const accounts = await getUserAccounts();

  return <div>{accounts.map(...)}</div>;
}
```

**优势**
✅ 更快的首屏加载
✅ 更好的 SEO
✅ 减少 JavaScript 体积

**相关概念**
→ [Client Component (客户端组件)](#client-component-客户端组件)
→ [Next.js](#nextjs)

---

### Session (会话)

**简单解释**
记住你是谁的"标签"。就像你去银行办业务，柜员给你一个号码牌,后续用这个号码确认身份。

**在项目中的作用**
Clerk 管理用户会话：
- 用户登录后创建 Session
- 存储在浏览器 Cookie 中
- 每次请求自动发送
- 服务器验证 Session 获取用户信息

**代码位置**
```javascript
// 获取当前会话
const { userId } = await auth();
if (!userId) {
  // 未登录
}
```

**相关概念**
→ [Clerk](#clerk)
→ [Authentication (认证)](#authentication-认证)
→ [JWT](#jwt-json-web-token)

---

### SQL

**简单解释**
数据库的语言。用来查询、创建、修改数据库数据。

**在项目中的作用**
虽然我们用 Prisma，但 Prisma 底层会生成 SQL：

```javascript
// Prisma 代码
db.user.findUnique({ where: { id: "123" } })

// 转换成 SQL
SELECT * FROM "User" WHERE "id" = '123'
```

**代码位置**
不需要直接写 SQL，Prisma 自动生成

**相关概念**
→ [Prisma](#prisma)
→ [Database (数据库)](#database-数据库)

---

### Supabase

**简单解释**
提供数据库服务的平台。就像你租一个"云端仓库"存数据，不需要自己搭建服务器。

**在项目中的作用**
- 提供 PostgreSQL 数据库
- 自动备份
- 提供管理界面
- 免费层可用

**代码位置**
- 连接配置：`.env` 中的 `DATABASE_URL`

**相关概念**
→ [PostgreSQL](#postgresql)
→ [Database (数据库)](#database-数据库)

**学习资源**
- [Supabase 官网](https://supabase.com)

---

### Suspense

**简单解释**
React 的"加载占位符"。数据还没来时显示 Loading，数据来了自动替换成真实内容。

**在项目中的作用**
```javascript
<Suspense fallback={<Loading />}>
  <DataComponent />  {/* 异步加载数据 */}
</Suspense>
```

**代码位置**
- Loading 页面：`app/(main)/dashboard/loading.jsx`

**相关概念**
→ [Loading State (加载状态)](#loading-state-加载状态)
→ [React](#react)

---

### Table (表)

**简单解释**
数据库中的一张"表格"。就像 Excel 里的一个工作表。

**在项目中的作用**

项目的 4 张主要表：
- **User** 表 - 存储用户信息
- **Account** 表 - 存储账户信息
- **Transaction** 表 - 存储交易记录
- **Budget** 表 - 存储预算设置

**代码位置**
- 表结构定义：`prisma/schema.prisma`
- 详细分析：`project-mastery/analysis/01-database-analysis.md`

**相关概念**
→ [Model (模型)](#model-模型)
→ [Database (数据库)](#database-数据库)

---

### Tailwind CSS

**简单解释**
"原子化" CSS 框架。不写传统 CSS，而是用小的实用类名组合样式。

**在项目中的作用**

传统 CSS vs Tailwind：
```html
<!-- 传统方式 -->
<div class="card">...</div>
<style>.card { padding: 1rem; border-radius: 0.5rem; }</style>

<!-- Tailwind 方式 -->
<div class="p-4 rounded-lg">...</div>
```

**代码位置**
- 配置：`tailwind.config.js`
- 全局样式：`app/globals.css`
- 所有组件都使用 Tailwind 类名

**相关概念**
→ [CSS](#css)

**学习资源**
- [Tailwind 官方文档](https://tailwindcss.com)
- 提示：查文档时搜索"Tailwind [你想要的效果]"

---

### Toast

**简单解释**
屏幕上弹出的小提示。就像手机上的通知气泡，告诉你"操作成功"或"出错了"。

**在项目中的作用**
用户操作后的反馈：
- ✅ "账户创建成功"
- ❌ "金额格式错误"
- ⚠️ "请填写必填项"

**代码位置**
```javascript
// hooks/use-fetch.js:20
toast.success("操作成功");
toast.error(error.message);
```

**相关概念**
→ [用户反馈](#user-feedback-用户反馈)

---

### Transaction (交易)

**简单解释**
一笔收入或支出记录。就像你的银行流水。

**在项目中的作用**

包含信息：
- 💰 金额（amount）
- 📅 日期（date）
- 📝 描述（description）
- 🏷️ 分类（category：食品、交通、购物等）
- 📄 收据（receipt）
- 🔄 是否循环（isRecurring）

**代码位置**
- 数据模型：`prisma/schema.prisma:29-47`
- 创建交易：`actions/transaction.js:18-100`
- 详细分析：`project-mastery/analysis/01-database-analysis.md`

**相关概念**
→ [Account (账户)](#account-账户)
→ [CRUD](#crud)

---

### try/catch

**简单解释**
捕获错误的"安全网"。就像走钢丝时下面的保护网，出错了不会摔死。

**在项目中的作用**
```javascript
try {
  // 尝试执行可能出错的代码
  const data = await riskyOperation();
  return { success: true, data };
} catch (error) {
  // 出错了，处理错误
  console.error(error);
  return { success: false, error: error.message };
}
```

**代码位置**
所有 Server Actions 都使用 try/catch

**相关概念**
→ [Error Handling (错误处理)](#error-handling-错误处理)

---

### TypeScript

**简单解释**
JavaScript 的"严格版"。会检查变量类型，提前发现错误。

**在项目中的作用**
虽然文件是 `.js` / `.jsx`，但通过 JSDoc 注释获得类型检查：

```javascript
/**
 * @param {string} userId
 * @returns {Promise<Account[]>}
 */
async function getUserAccounts(userId) {
  // TypeScript 会检查参数和返回值类型
}
```

**代码位置**
- 配置：`jsconfig.json`
- Prisma 自动生成类型

**相关概念**
→ [JSDoc](#jsdoc)

---

### UI (User Interface)

**简单解释**
用户界面。你在屏幕上看到的所有东西：按钮、文字、图片等。

**在项目中的作用**
- 基础 UI 组件：`components/ui/`
- 页面 UI：`app/` 文件夹中的组件

**相关概念**
→ [Component (组件)](#component-组件)
→ [Radix UI](#radix-ui)

---

### useFetch

**简单解释**
自定义 Hook，简化数据请求。自动处理 loading、error、success 状态。

**在项目中的作用**
```javascript
const { fn, loading, error, data } = useFetch(createAccount);

const handleSubmit = async (formData) => {
  await fn(formData);
  // 自动显示 toast 提示
  // 自动处理 loading 状态
};
```

**代码位置**
- Hook 定义：`hooks/use-fetch.js`
- 使用示例：`components/create-account-drawer.jsx`

**相关概念**
→ [Hook](#hook-react-hooks)
→ [Server Actions](#server-actions)

---

### useState

**简单解释**
React 的"记忆功能"。让组件记住数据，数据变化时重新渲染。

**在项目中的作用**
```javascript
const [count, setCount] = useState(0);
// count: 当前值
// setCount: 修改值的函数

<button onClick={() => setCount(count + 1)}>
  点击次数: {count}
</button>
```

**代码位置**
所有客户端组件都可能使用

**相关概念**
→ [Hook](#hook-react-hooks)
→ [State (状态)](#state-状态)

---

### Validation (验证)

**简单解释**
检查输入是否正确。就像填表时，电话号码必须是11位数字。

**在项目中的作用**

使用 Zod 进行验证：
```javascript
const schema = z.object({
  email: z.string().email("邮箱格式错误"),
  amount: z.string().min(1, "金额不能为空"),
  date: z.date({ required_error: "请选择日期" }),
});
```

**代码位置**
- 验证规则：`app/lib/schema.js`
- 在表单中使用：React Hook Form + Zod Resolver

**相关概念**
→ [Zod](#zod)
→ [Form (表单)](#form-表单)

---

### Vercel

**简单解释**
网站托管平台。把你的项目部署到 Vercel，全世界的人都能访问。

**在项目中的作用**
- 自动部署（推送到 GitHub 自动部署）
- 提供 CDN
- 提供免费 HTTPS
- 自动优化性能

**代码位置**
- 配置：`vercel.json`（可选）
- 部署指南：`project-mastery/specifications/PROJECT_SPEC_CN.md` 第9章

**相关概念**
→ [部署 (Deployment)](#deployment-部署)
→ [CDN](#cdn-content-delivery-network)

**学习资源**
- [Vercel 文档](https://vercel.com/docs)

---

### Webhook

**简单解释**
反向通知。不是你主动问，而是对方有消息时主动告诉你。就像快递送达时的短信通知。

**在项目中的作用**
Inngest webhook：
- Inngest 触发定时任务时
- 调用项目的 `/api/inngest` 端点
- 执行相应的后台作业

**代码位置**
- Webhook 接收：`app/api/inngest/route.js`

**相关概念**
→ [Inngest](#inngest)
→ [API](#api-application-programming-interface)

---

### Zod

**简单解释**
数据验证库。定义数据应该长什么样，自动检查是否符合要求。

**在项目中的作用**

定义验证规则：
```javascript
const accountSchema = z.object({
  name: z.string().min(1, "账户名称必填"),
  type: z.enum(["CURRENT", "SAVINGS"]),
  balance: z.string()
    .regex(/^\d+(\.\d{1,2})?$/, "金额格式错误"),
});
```

**代码位置**
- 验证规则：`app/lib/schema.js`
- 详细示例：`project-mastery/specifications/PROJECT_SPEC_CN.md`

**相关概念**
→ [Validation (验证)](#validation-验证)
→ [Schema](#schema)

**学习资源**
- [Zod 文档](https://zod.dev)

---

## 🎯 使用建议

### 快速查找技巧

1. **按首字母分组** - 术语按字母排序，快速定位
2. **使用浏览器搜索** - Ctrl+F (或 Cmd+F) 搜索关键词
3. **关注"相关概念"** - 跟随链接深入学习

### 学习路径建议

**初学者路径**：
1. 先看基础概念：Component → React → Next.js
2. 理解数据流：Database → Prisma → Server Actions
3. 学习交互：Form → Validation → useFetch

**遇到问题时**：
1. 在词典中查找不懂的术语
2. 阅读"在项目中的作用"部分
3. 查看"代码位置"实际例子
4. 跟随"学习资源"深入学习

---

**最后更新**: 2025-11-16
**词条数量**: 80+
**覆盖范围**: 前端、后端、数据库、部署、安全

💡 **提示**: 把这个文档加入书签，遇到不懂的术语随时查阅！
